# Documentation

This directory contains the canonical source material the project was built from, plus reference documents the rest of the codebase points to.

## Single source of truth — where each kind of content lives

To keep the docs efficient and stop the same rule from sprawling across files (and going stale in some), every kind of content has **one canonical home**. State it there; everywhere else, *link* — don't re-explain.

> **Before you add a row to this table or a file to this directory: a new document is the last resort** (CLAUDE.md Critical rule #7, fourth obligation). Content graduates into a home that already exists — a rule in `CLAUDE.md`, a KDD in `PLANNING.md`, a section of `architecture.md`/`testing.md`, a row in `TASKS.md`. A **new file** requires that no existing home fits, and the commit message must name the homes considered and why each failed. The rule is stated *here*, at the place new rows get added, because that is where the decision is actually made — four new top-level docs appeared inside 71 minutes on 2026-08-06, and a recurring activity's sheet (the rig session) had reached five near-duplicates before it was collapsed to one. A stale sheet is **rewritten**, not joined by a sibling; the superseded original goes to [`archive/`](archive/README.md) with a row in its graduation map.

| Content | Canonical home |
|---|---|
| Locked coding conventions & critical rules | `CLAUDE.md` → *Code conventions* / *Critical rules* |
| How the maintainer works / project values | `CLAUDE.md` → *Working with the maintainer* |
| CI / release & build operations | `CLAUDE.md` → *Project operations* |
| Commit & PR conventions (this repo) | `CLAUDE.md` → *Project operations → Commit & PR hygiene* (general mechanics: `docs/github-workflow-sop.md`) |
| Architectural decisions + rationale (KDD-NN) | `PLANNING.md` |
| Module map & per-module responsibility | `PLANNING.md` |
| Layered design, patterns, engineering lessons, extension recipes, packaging/release/security | `docs/architecture.md` |
| Testing strategy, taxonomy, institutional rules | `docs/testing.md` |
| Manual & release testing (acceptance run + gated cases + tester matrices) | `docs/test-plan.md` |
| Every hardware test still outstanding, as a fillable run sheet | `docs/hardware-test-checklist.md` |
| The current rig session — what to WATCH on screen, plus one script | `docs/rig-session.md` |
| What ships in the next release, and what is gated on the rig package | `TASKS.md` → *Next release — gated on the rig package* |
| How a failure becomes something a person can act on (error reporting) | `docs/architecture.md` §3.7 + §3.7a; the recipe is §4 *Add a failure path* |
| AppImage build/test procedure, and the release-signing ritual | `docs/architecture.md` §6.1 / §6.2 |
| The rules governing the cyanrip seam, shared byte-identical with the fork | `docs/seam-rules.md` |
| Every flag, type, argument and meaning crossing the seam — the one table | `docs/seam-commands.md` |
| Dependency pins, dates, licenses, retirement log | `DEPENDENCIES.md` |
| User-facing changes | `CHANGELOG.md` |
| Active task queue | `TASKS.md` |
| What happened each session (chronology) | `docs/session-log.md` |
| Install / usage docs | `README.md` |

A lesson legitimately appears in **two** places: a one-line *rule* in its canonical home, and a dated *entry* in `docs/session-log.md` recording how it arose (the **graduation rule** — distillation up, chronology down). Anything beyond that is duplication to delete.

### Doc version stamps

Every Markdown doc (excepting the ready-to-paste upstream issue/PR bodies in `scripts/cyanrip/`, where a Platterpus footer would pollute the paste) ends with a **`*Last updated for Platterpus v0.6.12b3.*`** footer — the release the doc's content was last revised for — so a reader can gauge its currency at a glance. This is part of *documentation currency* (CLAUDE.md Critical rule #7): **when you change a doc, bump its stamp to the current `__version__` in the same commit.** The stamps were seeded (2026-07-07) from git history — the `__version__` in effect at each file's last content commit — so an old stamp means the doc simply hasn't needed a change since, not that it's unmaintained.

**Enforced since v0.5.0** by `tests/test_doc_version_stamps.py` (gating, runs in the CI `test` job): every tracked doc must carry exactly one footer, no footer may claim a version newer than `__version__`, and — the piece that keeps stamps honest across a release — any doc whose *content* changed since the latest release tag must be stamped with the *current* `__version__`. That last rule means the release-prep version bump itself forces a restamp of every doc the cycle edited, so stamps can no longer lag the release their content actually ships in (which happened once: the entire v0.5.0 cycle shipped stamped v0.4.24 — caught by the maintainer on the public README, 2026-07-21). The test compares each doc's **footer-stripped** content against the release tag, so a stamp-only bump doesn't count as a change (that keeps the bump from cascading across untouched docs every release — an old stamp still means "content unchanged since"). The test can't judge "meaningful," so any *content* edit requires the stamp bump — it's one line, in the same commit.

## Source documents (anchor for "rebuild from scratch")

These two files, together with the top-level `CLAUDE.md`, `PLANNING.md`, `TASKS.md`, `DEPENDENCIES.md`, and `README.md`, are the full context needed to reproduce the project from a clean slate.

| File | What it is | Authority on |
|---|---|---|
| [`platterpus-research-brief-v2.1.md`](platterpus-research-brief-v2.1.md) | The original requirements brief — every P0/P1 feature, every constraint, every scope decision started here. | **Requirements and scope.** When PLANNING.md and the brief conflict, the brief *as amended by the maintainer-approved KDDs* wins on requirements; PLANNING wins on implementation. |
| [`platterpus-session-start.md`](platterpus-session-start.md) | The bootstrap instructions a fresh Claude Code session followed to produce the initial five top-level files — and (Step 0, optional) the paste-verbatim Research-mode prompt for refreshing the tool-choice validation against the brief. | **Initial repo state + the bootstrap procedure, and how to refresh the tool-choice research.** Re-run it against a clean repo to re-derive the planning artifacts. |

> **About the `compass_artifact_*.md` Research validation file:** the original v1 brief produced a compass-artifact research validation in a Claude Research session; the user could not locate it when this project was bootstrapped, so the project proceeded against the brief alone (see CLAUDE.md "Companion documents"). If the session-start Step 0 rerun prompt is ever invoked, save the resulting `compass_artifact_*.md` into this directory.

## Reference documents

| File | What it is |
|---|---|
| [`architecture.md`](architecture.md) | **Architecture & contributor guide** — the layered design and dependency direction; the core patterns *with the why and the hard-won lessons* (adapter layer, the never-block-the-GUI-thread discipline + worker mechanics, subprocess rules, never-raise parsers, the dependency subsystem, the MainWindow mixin decomposition, error/logging); step-by-step **extension recipes**; the testing contract; packaging/building/releasing; security & licensing hygiene; and the architectural future-directions horizon. **Start here to extend the program.** (Absorbed the former `best-practices.md`, and — 2026-08-06, under CLAUDE.md rule #7's new-file obligation — three more: `error-reporting.md` → **§3.7a** the error-reporting design of record beside the §3.7 rules it restated, with its recipe as §4 *Add a failure path*; `appimage-testing.md` → **§6.1**; `release-signing.md` → **§6.2**, the maintainer's offline-key `minisign` ritual, which `CLAUDE.md` → *CI / release* points at directly because it is run under release pressure and its fail-closed arming transition must not be half-read.) |
| [`testing.md`](testing.md) | **Testing strategy & standards** — the trophy + a real-hardware gate, the five-tier case taxonomy (easy/medium/hard/edge/unexpected), when to use property-based / golden / fault-injection / mutation testing, the institutional rules (every bug gets a regression test; parsers never raise; coverage gate ratchets up), and a Definition of Done. Portable to sibling projects. |
| [`test-plan.md`](test-plan.md) | **Manual & release testing** — the end-to-end clean-cycle acceptance run (uninstall → fresh install → drive setup → rip → verify), the **EAC output-parity** check (with the per-track CRC baseline), the **Linux-distro** + **problem-permutation** matrices for onboarding testers, *and* the deep single-feature gated cases, Tests 1–14 (CTDB verify CRC, the drive-setup wizard screens, GUI screenshot, Picard UX, the cyanrip parity record + `-Z` convergence, multi-format proof, read-effort/CD-Extra/companion-log cases). Run one at a time and record results. (Absorbed the former `release-testing.md`.) |
| [`hardware-test-checklist.md`](hardware-test-checklist.md) | **The fillable run sheet for every hardware test still outstanding** on the Bazzite + BDR-209D rig — not just the newest release's. Every step written out and copy-pasteable, grouped as: setup (§0) → this release's new features (§A) → shipped-but-still-unproven from earlier releases (§B) → never-exercised-on-hardware risk areas (§C: missing optional dep, probe cancel, settings persistence, contradictory settings, upgrade-over-existing, UI sweep) → by-hand probes that answer an open design question (§D) → the INDEX-00 / cyanrip-build test last (§E — it swaps the ripper binary). Test IDs are **stable across releases and retired when they pass**, so gaps in the numbering are deliberate and the sheet shrinks as the rig proves things; what passed and when lives in `session-log.md`. Updated in place each cycle; `test-plan.md` stays the full reference. |
| [`rig-session.md`](rig-session.md) | **The current rig session — one sheet, rewritten when the pair moves.** Names the exact pairing in its header (today `v0.6.4b11` + cyanrip `9048082`) and is written to be *read at the rig*: which screens to look at before a disc goes in, the two things to watch with your own eyes during the rip because no artifact reports them (the stall warning and the ETA during a secure re-read), the one `grep` that closes the open handshake round, one unattended command, and exactly which files to send back. **Supersedes four build-specific sheets** (`b9`, `b10`, `c5fb909`, `f5e11ba`) now in [`archive/`](archive/) — five near-duplicates had accumulated, one per app-build/ripper-pin pairing, and the maintainer asked for the pile to be combined. Carries an explicit **what is still not fixed** section and, unusually, a *what turned out not to be true* one: two of the five things the last session's screenshots suggested were refuted on investigation, and saying so is cheaper than shipping a fix for a bug that does not exist. |
| [`seam-rules.md`](seam-rules.md) | **The rules governing the cyanrip seam — one file, byte-identical in both repos, which neither project owns** (same mechanism as `handshake-protocol.md`). Every rule is tagged **`[BOTH]` / `[PLATTERPUS]` / `[CYANRIP]`** and both sides read all of them, including the ones binding only the other: a consumer who does not know what the provider guarantees re-derives it wrongly, and a provider who does not know what the consumer parses changes a line thinking it is free. Seven universal rules (both directions validated; a new route delegates rather than restates; received text is external input; every elision counted; **neither half is evidence for the other**; each side validates independently as a double check; exit codes tri-state), three binding only Platterpus, three only the fork. **§4 is the point of the document**: every value crossing the seam with its **type** and what it is checked for, in both directions — plus the values that are neither side's, because album and track titles arrive from MusicBrainz as arbitrary Unicode. Conformance is declared (`SEAM-RULES-VERSION: 1`) and *a rule you have not implemented is not a rule you may cite*. |
| [`seam-commands.md`](seam-commands.md) | **THE table, and its only purpose is to be that** — every command, flag, argument, type, range and meaning crossing the Platterpus↔cyanrip seam, one file at the same path in both repos, **exchanged every round in both directions** so "nobody sent it" is never confusable with "nothing changed". Every row carries a status from **each** side (`HAVE` / `EXPECT` / `NEED` / `WANT` / `NO` / **`?` = open, not passing**), because the interesting information is where they disagree — `-V` is the worked example: their table said no `-V` for a full round while every probe we shipped sent it, and a rejected flag exits non-zero, which every probe reads as *"the tool is not installed"*. Currently a **draft**: the type and range columns are hand-transcribed and carry a provenance warning until `emit_dependency_contract.py` generates them from the builder's own signatures, and the cyanrip column is `?` throughout because we have not asked yet. |
| [`eac-parity.md`](eac-parity.md) | **The single home for EAC parity — four investigations, consolidated 2026-08-03.** *Part A* (2026-06-27): can our output be bit-identical to EAC? Every deviation axis with its feasibility; the key finding is that bit-identical *audio* (AccurateRip CRC) is the achievable, meaningful goal — 12/14 already met — while bit-identical *files* are impossible across FLAC encoders and unnecessary. *Part B*: the two log formats field by field, anchoring [KDD-11](../PLANNING.md); its data is the EAC log at `tests/fixtures/rip_log_eac_reference.log`. *Part C* (2026-06-28): tracker acceptance via an emitted EAC log — technically possible through the reverse-engineered checksum and a hard **no**, because it forges EAC provenance — plus in-app CUETools/CTDB repair, feasible but a heavy .NET dependency that rewrites the master (CRC hardware gate cleared, KDD-16; maintainer appetite still pending). *Part D* (2026-07-29): measured against the real logcheckers, row by row. Headline: a cyanrip log can **never** pass — the gate is ripper *identity*, checked before any quality line — so closing every quality gap moves the score from unscoreable to unscoreable. Part D corrects an earlier claim in Part C, and records a change attempted and reverted (asserting `Make use of C2 pointers : No` from a secondary source, which a fabrication test correctly blocked). |
| [`dependency-contracts.md`](dependency-contracts.md) | **Dependency contracts** — the single reference for the exact arguments/flags/syntax Platterpus passes each external tool (cyanrip, flac, metaflac, ffmpeg, musicbrainzngs, CAA, CTDB, drive/reader control) and the output shape it parses back. The code-side counterpart to the "validate every input and every dependency output" rule; keep it in step with the adapters. |
| [`session-log.md`](session-log.md) | **Chronological session history** — what each Claude Code session built, decided, and learned (newest first). The project's institutional memory; durable lessons graduate from here into the docs above. |
| [`cyanrip-fork.md`](cyanrip-fork.md) | **Our fork: why, and how it is kept sane — consolidated 2026-08-03.** *Part A* — research/options, living and long-horizon: the feasibility of forking and/or combining whipper + cyanrip and maintaining our own engine, with the licensing analysis that gates all of it, the option menu, and the decision gates. Revisits KDD-18's "never fork" stance; a commitment needs a new KDD. Part A §10 is the per-gap, license-compatible open-source option menu. *Part B* — the executable half (2026-07-08): how `rmccann-hub/cyanrip` is maintained as upstream `master` plus a small rebased patch set, each sent back upstream as a focused PR and dropped from the fork once merged. Carries the two prepared contributions with verbatim bug analysis, minimal fixes and ready-to-paste issue text — **(1)** the `-a`/`-t` colon-parsing fix (retires our U+2236 workaround) and **(2)** full libavcodec encoder args — plus building and consuming the fork in the `ripping` container and the re-merge discipline. Companions: [`../scripts/cyanrip/verify-meta-colon.c`](../scripts/cyanrip/verify-meta-colon.c) (the ASan/UBSan-verified C harness proving the colon fix) and the one-command kit in `scripts/cyanrip/`. For the **binding release protocol** see [`cyanrip-handshake.md`](cyanrip-handshake.md). |
| [`cyanrip-handshake.md`](cyanrip-handshake.md) | **Release protocol (binding):** the **bidirectional** handshake between this project and the cyanrip fork. Two files per round, in both directions, and **neither project ships until both have verified the other's**. Specifies what each side must send, the shared rigour bar, and the scope table for when a handshake is required at all. Exists because both sides have been wrong about the other at least once — a one-directional report is a claim, a handshake is a claim plus a check. Enforced by `tests/test_handshake_protocol.py`. |
| [`cyanrip-upstream.md`](cyanrip-upstream.md) | **What we want from cyanrip, and how it gets there — consolidated 2026-08-03.** *Part A* (2026-07-31): the ranked ask list, every improvement with the evidence motivating it, an explicit set of asks we decided **not** to make, and — §6 — corrections to our *own* record found while assembling it. *Part B* (2026-07-07): the contributor-facing roadmap turning those asks into merged pull requests, in priority order, with per-PR feasibility, effort and acceptance odds. Answers "do I need to be a collaborator?" (no — fork + PR), marks the honest DO-NOW (cdrdao integration, no upstream PR) against the skip-for-now items, and keeps a signed EAC checksum permanently out of scope. |
| [`cyanrip-consumer-contract.md`](cyanrip-consumer-contract.md) | **GENERATED — do not hand-edit.** Our half of the cyanrip dependency contract: every log line the parser reads (with its scope and pattern), every line we knowingly *ignore* with the recorded reason, and every flag the argv builder sends. Written by [`../scripts/emit_dependency_contract.py`](../scripts/emit_dependency_contract.py) from the parser's own enumeration tables plus a real call to the builder, so it cannot describe behaviour we do not have — which is exactly how we once told the fork a line was stdout-only when it was not. The fork supplies the mirroring **provider** contract; two halves, one seam. Staleness is a test failure (`tests/test_dependency_contract_emitted.py`), not a review item. |
| [`script-language.md`](script-language.md) | **GENERATED — do not hand-edit.** The complete reference for the UI test-script language: the verb dictionary with arity and safety class, every dialog `open` accepts, every numeric limit, every settable config field, the cyanrip-passthrough rules, the eight rules for writing a script that is worth running, a worked annotated example, the recipe table, how to add a verb, and what each outcome means. **Both audiences in one file** — prose for a person, and the same facts as a fenced JSON block for a machine, emitted from one pass over one set of objects so they cannot disagree. Written by [`../scripts/emit_script_language.py`](../scripts/emit_script_language.py) from the vocabulary table, the runner's live limit constants and `Config`'s own fields, so it is **truthful for the version it ships with** (maintainer requirement, 2026-08-07) — it carries that version in both the footer stamp and a machine-readable field. Staleness is a test failure (`tests/test_script_language_emitted.py`), not a review item. **Start here to learn the language or to write a new test.** |
| [`rig-scripts/`](rig-scripts/README.md) | **Runnable hardware-session scripts**, plus the three-step shape of a session (`--rig-session` before the disc, a script with the disc in, `--audit-rips` after). Carries the current `police-rerip.txt`, the guidance on the two lines to change per disc, and the ⚠ table separating `-x` (the fork's cache probe) from `-O` (overread — confirmed to hang the BDR-209D for ~23 minutes), because that confusion is a hardware hazard rather than a documentation nit. The language itself is [`script-language.md`](script-language.md). |
| [`handshake-protocol.md`](handshake-protocol.md) | **SHARED — the same document in both repositories, and neither project owns it.** The wire format both release gates read: the column-0 header block (`HANDSHAKE-PROTOCOL`/`-ROUND`/`-LAP`/`-FROM`/`-VERDICT`/`-APP-VERSION`/`-RIPPER-VERSION`/`-PIN`), the closed verdict vocabulary, the extra fields a **close** requires (both verdicts, both versions, both pins, `HANDSHAKE-TESTED`), the optional `HANDSHAKE-TEST-PIN` (§6a — the field that breaks the deadlock where a close needs evidence only obtainable by installing the build under review) and the pre-release carve-out (§6b), and a **20-row conformance table with stable row IDs `C1`–`C20`** each side tests against — ours is `tests/test_handshake_conformance.py`, one test per ID, with the ID set *derived from this file's table* rather than hardcoded. Adopted verbatim from the cyanrip fork in round 7 lap 4 and **re-synced in lap 31**: our copy had not been touched since lap 4 while theirs had absorbed laps 6, 7 and 8 — including §6b, a mechanism *we* proposed and never wrote into the shared file. Cite the row IDs in any disagreement, so both projects are provably discussing the same row and not the same paraphrase. **Editing it unilaterally is a protocol version bump** both sides must ship before the next close — and note that this re-sync was *not* accompanied by a bump on either side, which is the drift the versioning rule exists to catch; lap 31 raises it. Our implementing gate is `scripts/handshake.py`; the local note that used to sit at the top of the shared file lives here instead, because a Platterpus-only comment inside it broke the byte-identity the file exists to have. The protocol's own history and rationale live in [`cyanrip-handshake.md`](cyanrip-handshake.md); this file is just the language. |
| [`handshake/`](handshake/README.md) | **The correspondence record for the binding cyanrip handshake** — every round as sent, returned and verified, in `outbound/`, `inbound/` and `verified/`. The protocol is [`cyanrip-handshake.md`](cyanrip-handshake.md); these are the artifacts it produced, committed so the record survives the session that made it. `python scripts/handshake.py --status` reads this directory and is what decides whether a release is allowed: a round is OPEN until our verification file *declares GO*. See `handshake/README.md` for the round-by-round map. |
| [`ctdb-crc-algorithm.md`](ctdb-crc-algorithm.md) | **Reference (2026-07-07):** the bit-exact CueTools DB per-disc CRC algorithm reconstructed from the CueTools LGPL source — it *is* a plain `zlib.crc32` over a fixed-front/length-dependent-back trim, with the **±5879**-frame offset sweep (CTDB's range, wider than AccurateRip's ±2939) and the `crc32_combine` fast path. The spec behind `src/platterpus/ctdb/crc.py` + `calibrate.py`; anchors KDD-16. |
| [`github-workflow-sop.md`](github-workflow-sop.md) | **Contributor/upstream-PR reference (filed 2026-07-07):** general GitHub ecosystem SOP — identity/SSH, cloning vs forking, GitHub Flow + branch naming, atomic commits + the 7 commit-message rules, PR anatomy, merge strategies + branch protection, conflict resolution. A preface reconciles it with *this* repo's policy (CLI + GitHub MCP not Desktop, fast-forward-only proxy, `claude/…` branch, squash-merge, lowercase `type(scope):` subjects with no 50-char cap); `CLAUDE.md` wins where they differ. Most useful as the playbook for contributing *upstream* (see `cyanrip-upstream.md`). |
| [`ux-design-principles.md`](ux-design-principles.md) | **Living UX guidance (2026-06-28):** the trust-first design principles distilled from deep-research on why EAC made bit-perfect ripping usable — verification as a first-class UI object, safe-by-default, progressive disclosure, localized failure, two-logs/tamper-evident, per-drive profiles, accessibility — plus a gap analysis vs Platterpus and the ranked UX backlog. The *why* behind the UX; the bar new rip features are held to. |
| [`manual-ctdb-repair.md`](manual-ctdb-repair.md) | **Power-user how-to (2026-07-21):** the manual CUETools/`ctdb-cli` CTDB **repair** workflow for a track that stays "partially accurate (450)" after re-rips — when to reach for it, both tool routes (.NET `ctdb-cli` headless / CUETools GUI under Mono), the fold-back-into-the-library steps, and the safety rules. Assembled strictly from the feasibility + parity-investigation record; steps never executed on project hardware are marked *(unverified)*. In-app repair stays parked (KDD-14 Phase 2). |
| [`archive/mp3-wav-support-2026-06.md`](archive/mp3-wav-support-2026-06.md) | **Design-of-record for multi-format output (SHIPPED 2026-06-26, KDD-22; archived 2026-08-06):** the FLAC-master + WavPack/MP3/WAV-derived model, per-format parity semantics, the verified encoder args (FLAC `-8 -e -p`, MP3 VBR `-V0`, WavPack lossless) and the transcode-always decision. Flipped Critical Rule #4 (FLAC is now the default/master, not the only format). Indexed here rather than only in `archive/README.md` because `src/platterpus/adapters/transcode.py`, `config.py` and the user README all cite it by name for the encoder-argument rationale. Its **one open item** — embedding cover art inside a `.wv`, which needs the standalone `wavpack` tool — was graduated to `TASKS.md` P2 when it was archived. |
| [`archive/audit-2026-07-21.md`](archive/audit-2026-07-21.md) | **Point-in-time documentation-audit record (2026-07-21):** the full-docs audit (239 findings) — the systemic patterns, the before→after doc map, and the open questions; its unexecuted consolidation plan lives in `TASKS.md` → P1 Documentation backlog. |
| [`archive/`](archive/README.md) | Retired point-in-time investigations (ecosystem audit, read-offset, upstream-modification/CTDB spec) **plus external reference material** (the EAC archival master guide). Their durable conclusions have graduated into KDDs / DEPENDENCIES / adapter comments — see `archive/README.md` for the map. |

## Where the rest of the project context lives

Outside this directory:

| File | What it covers |
|---|---|
| [`../CLAUDE.md`](../CLAUDE.md) | Persistent rules and conventions; locked rules section; project operations |
| [`../PLANNING.md`](../PLANNING.md) | Architecture, directory tree, per-module responsibilities, adapter designs, dependency-manager design, keyed design decisions (KDD-01 … KDD-34) |
| [`../TASKS.md`](../TASKS.md) | Active task checklist — P0 (T01-T32), P1.1 (install/uninstall ease), P1 (broader backlog), P2 (future), Out of scope |
| [`../DEPENDENCIES.md`](../DEPENDENCIES.md) | Pinned versions, last upstream release dates, retirement-review log |
| [`../README.md`](../README.md) | User-facing install instructions, troubleshooting, EAC comparison |
| [`../SECURITY.md`](../SECURITY.md) | Security policy: vulnerability reporting, supported versions, supply-chain posture |
| [`../CHANGELOG.md`](../CHANGELOG.md) | The one authoritative record of user-facing changes (SSOT table above) |
| [`../output_reference/README.md`](../output_reference/README.md) | The EAC/cyanrip parity-proof baselines (text artifacts only — Critical rule #8) |
| [`../scripts/cyanrip/README.md`](../scripts/cyanrip/README.md) | The cyanrip upstream-contribution kit (executes `cyanrip-fork.md`) |
| [`../build/python-appimage/README.md`](../build/python-appimage/README.md) | AppImage build recipe details |

## Rebuild-from-scratch checklist

If you needed to start over with a fresh git repository:

1. **Place these files at repo root:**
   - `CLAUDE.md` (copy verbatim from the user's CLAUDE.md template — the rules section is locked)
   - `PLANNING.md`, `TASKS.md`, `DEPENDENCIES.md`, `README.md` (produced by Claude Code Step 3 per `platterpus-session-start.md`)
2. **Place these files in `docs/`:**
   - `platterpus-research-brief-v2.1.md`
   - `platterpus-session-start.md`
3. **(Optional but recommended after 6+ months) Re-run Research validation:** follow `platterpus-session-start.md` **Step 0**, save the result as `docs/compass_artifact_<hash>_text_markdown.md`.
4. **Boot a fresh Claude Code session,** attach the brief + session-start + (if present) compass artifact + CLAUDE.md, and ask it to execute `platterpus-session-start.md`. The session reproduces PLANNING.md, TASKS.md, DEPENDENCIES.md, README.md from scratch and then begins executing the task list.
5. **Subsequent sessions** follow CLAUDE.md as the primary instruction document, using TASKS.md to track what's next.

---

*Last updated for Platterpus v0.6.12b3.*
