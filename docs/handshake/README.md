# Handshake correspondence with the cyanrip fork

Every file exchanged with the cyanrip fork, both directions, committed so the
record survives the session that produced it. The protocol itself is
[`../cyanrip-handshake.md`](../cyanrip-handshake.md); the tool that enforces it
is `scripts/handshake.py`.

```
outbound/round-NN-lap-LL.md   what Platterpus sent    (protocol §3)
inbound/round-NN-lap-LL.md    what the fork sent back (protocol §4)
verified/round-NN-lap-LL.md   our verification of it  (protocol §2, step 5)
```

**Three files here are NOT rounds and must never be counted as one.** Each side's
**standing status** — what the other can assume *between* rounds, and what a fresh
session on either side reads to get up to speed. They carry no `HANDSHAKE-*` wire
headers, which is why the lap-naming test does not bind them, and they are
**rewritten in place** rather than gaining a sibling per revision (a stale standing
status is worse than none — the opposite rule from the correspondence above, which
is append-only). Rounds are the formal channel and they cost something: S-13 fixes
a round's close conditions at lap 1, and an open round blocks both sides' releases.
Between rounds each side still needs somewhere to say where it is.

```
outbound/platterpusstatus.md      ours   — undated; the as-of is in its heading
inbound/cyanripstatus<date>.md    theirs — filed under the name they used
                                         (TWO of these on disk: 20260821, 20260913)
```

## File naming — `round-NN-lap-LL.md`

**Both projects use this.** Agreed 2026-08-04 on a maintainer directive (*"agree on a
naming convention for the handshake files and both use it"*), proposed to the fork in
lap 17, and enforced on our side by `tests/test_handshake_file_naming.py`.

| rule | why |
|---|---|
| The name states the **round** and **lap** from the file's own wire header | The filename becomes a *second description* of a fact the header already declares — safe only because a test asserts the two agree |
| Zero-padded to **two** digits (`lap-09`, not `lap-9`) | So a human scanning the directory reads it in order. Nothing in the tooling sorts on the string — see *Ordering* below — but `lap-9` sorting after `lap-10` in `ls` is its own trap |
| **Direction comes from the directory**, never the name; `HANDSHAKE-FROM` must agree with it | One fact, one place |
| **No amendment letters.** An amendment is a new lap | A lap number is a fact both sides can state; "the next free letter" is a fact only the filer knows |
| Generate it with `handshake_filename(round, lap)` | Never hand-typed — a typed name is a third description |
| Artifacts: `round-NN-lap-LL-<kind>-g<build>.<ext>` | `<build>` is the commit the artifact's **own banner** asserts, not the commit a lap file names it by. Those differ (see below), and only the banner is derivable from the artifact's content |
| Transport envelopes: `round<NN>lap<LL>FROMplatterpusTOcyanrip.md` — **no hyphens** | A different convention on purpose; see below. *Documented here as `round<NN>lap<LL>platterpus.md` until 2026-09-13, while `emit_envelope.NAME_TEMPLATE` had carried the direction-bearing form since round 16 — this README's own rule says generate it, never hand-type it, and this README hand-typed it.* |

### Transport envelopes — `round<NN>lap<LL>FROMplatterpusTOcyanrip.md`

> **SUPERSEDED AS A TRANSPORT, 2026-09-13.** Laps now travel by git: each side
> commits to its own public repo and the other reads it (`CLAUDE.md` Critical rule
> #12, *Laps travel by git*). The envelope existed so an operator could send one
> attachment, and a commit carries every byte losslessly with a sha covering all of
> them — strictly stronger than a per-part hash in a wrapper. The section is kept
> because nineteen envelopes are committed here and a reader needs to know what
> they are; it is **history, not procedure**. What has *not* moved with laps: rig
> scripts and evidence bundles still cross by hand, so the cross-machine filename
> rule keeps a live subject.

The envelope is one file wrapping several laps verbatim so the operator sends one
attachment (*"there should only be one file moving forward, unless the second is a
script file to run"*, 2026-08-15). It is **not** a lap, and its name has two jobs the
lap convention cannot do:

* **It must not match `round-*.md`**, the glob both projects' gates use to collect
  laps. The envelope carries a wire header per part, so a matching name could be
  resolved as a lap and displace the round's real latest one — deciding a verdict read
  off a container. `round09…` has no hyphen after `round`, so it cannot match on a
  case-sensitive or a case-insensitive filesystem.
* **It crosses machines by hand**, through a chat client and a file manager, so it
  follows CLAUDE.md → *Artifact filenames that cross machines*: lowercase ASCII letters
  and digits only, numbers zero-padded. That rule was written after a rig run was lost
  to `round08joint.txt` vs `round-08-joint.txt`.

Generate it with `emit_envelope.envelope_filename(round, lap)` — never hand-typed. The
literal drifted three times in one session (`round08platterpusbundle` →
`round09platterpusenvelope` → `round09lap06platterpus`) because nothing stated the
pattern and no test checked it; the operator noticed before any gate did. The name is
now derived from the header of the lap the envelope leads with, so it cannot disagree
with the contents, and `tests/test_handshake_file_naming.py` pins both properties.

**Why it changed.** The old scheme was `round-N` plus the next free letter, and the
letter encoded nothing:

* `inbound/round-7f.md` was **lap 12** while `verified/round-7f.md` was **lap 10** —
  the same suffix meant different laps depending on the directory;
* `inbound/round-7d.md` and `verified/round-7d.md` were *both* lap 7, by coincidence;
* filing a received file meant picking "the next free letter", and picking wrong
  **overwrote a previous lap**. That happened: lap 12 was copied over `round-7c.md`,
  which was lap 4, and had to be restored from git.

**A file that declares no lap is lap 1 of its round** — the fork's rule, adopted in
lap 19 after their lap 18 showed the two projects had picked different numbers (we had
0) for one convention. Theirs is more correct: a round's pre-lap-header file *is* that
round's first lap, and calling it lap 0 invents a lap that never existed.

So the sole no-lap file of a **lap-numbered round** takes `lap-01`, because that name
states a derivable fact. Two conditions keep the exemption from becoming a hole: the
name must claim lap **1** specifically, and it must be the round's **only** no-lap file
— which is why round 6's three amendment files keep their legacy names. A round "uses
laps" iff some file in it declares one; rounds 1–6 are grandfathered wholesale.

**An ambiguous `HANDSHAKE-LAP` (declared twice, different values) sorts LAST.** Also the
fork's rule, and it closed a real hole on our side rather than merely aligning us: we
used to fall back to the filename, so an ambiguous file sorted at its *named* lap, a
later valid file was read as the newest, and the ambiguity was never examined by the
gate. Sorting it last makes it the file the verdict is read from, at which point the
header check refuses it.

## Artifacts — `artifacts-round-NN/` (round 7 and earlier) and `artifactsroundNN/` (round 8 on)

Rig artifacts and derived records for a round live in a per-round directory. **The naming
convention changed at round 8** — see below — so both spellings exist and neither is
retro-renamed, because a path already cited in committed correspondence is a string other
documents depend on.

- **`artifacts-round-07-lap-29/`** — the older form, `round-NN-lap-LL-<kind>-g<build>.<ext>`,
  `<build>` being the commit the artifact's **own banner** asserts, never the one a lap file
  names it by. Holds the 2026-08-04 rig rip (log, auto-fix addendum, cue, rendered
  EAC-compatible log, JSON report) plus `rig-session-results-c5fb909.md`, the derived record
  that says which artifact settled which claim.
- **`artifactsround08/`** — **two** rig runs, distinguished by filename prefix, because they
  differ in the one variable round 8 is about:
  - `round08…` — the 2026-08-13 run on ripper build **`g2ce8993`**, which is **NOT** the
    `ddf7ac3` under review. Evidence about one build is not evidence about another, and the
    directory's `README.md` says so at the top. Kept rather than superseded: it is the only
    hardware evidence about that build.
  - `round08pin…` — the 2026-08-15 run on **`ddf7ac3`, the pin under review** — round 8's
    close-condition-1 rip, and what lap 10's `GO` rests on. Banner verified before and after,
    `--rig-check` → `OK ripper/handshake approved`. Its cue is also the artifact that
    confirmed the fork's `-l` pre-gap-marker disclosure *and* carries the control case that
    bounds it; `tests/test_cue_validate.py` re-derives every number from it.

  Each run holds: the cyanrip log, the cue, our JSON report, the ui-script transcript and
  report, the `--rig-check` manifest, the argv probe, and the app log. The 2026-08-13 set
  additionally holds `--doctor`, the rig's config and its drive profile.
- **`artifactsround26/`** — the 2026-09-24 real test on **`df91ae7`**, round 26's reviewed
  pin, through our 0.6.55: every text member of the operator's bundle (sha256
  `f1486417…`), 46 files. The fork filed the same bytes under its own names at
  `cyanrip@9764970:docs/rig-2026-09-24-df91ae7/`; 40 of our 46 have identical git blob
  ids to theirs, and the other six are our rip reports, which they left to us. The
  directory's `README.md` maps each file to its tarball member and to the fork's copy.
- **`artifactsround27/`** — the 2026-09-26 **quick** run on **`221a1df`**, round 27's
  reviewed pin, through our 0.6.60, standing in for round 27's Full acceptance by the
  operator's override of R1: every text member of the operator's bundle (sha256
  `827d43da…`), 15 files. The fork filed the same bytes at
  `cyanrip@29cae9e:docs/rig-2026-09-26-221a1df-quick/`, and all 15 have identical git blob
  ids to theirs. The directory's `README.md` maps each file to its tarball member and to the
  fork's copy. **The same directory holds the 2026-09-26 04:13 UTC Full run on the same
  pair**, as `round27full*`: 48 text members of bundle `7b6b45d3…`, 320 of 320 steps,
  and the run §0.1 asked for before the override.

- **`outbound/artifacts/lsl-amendments-1.md`** — **a proposal, not a lap**: our answer to
  the fork's round 27 lap 6 S23 (sha256 `72a4c65afde184f2…`, 17,666 bytes; revised the same day with F4,
  before the fork was pointed at it). LSL, the fork's lap statement language, is the base; this file carries what
  our second implementation of it found (F1–F4), eight amendments for LSL 2 (A1–A8), and
  three header items for protocol v7. Released by the operator 2026-09-26. Its checker is
  `scripts/laplang/` (`scripts/lap_language.py check`), and its worked example is our round
  27 lap 5 rewritten (`tests/fixtures/lap_language_round27_lap05.md`). It replaces
  *lap language 1*, a language of our own built the same day and never sent.

**Names are lowercase ASCII letters and digits only from round 8 on** (`CLAUDE.md` →
*Artifact filenames that cross machines*). These files leave the repo and come back, and two
naming conventions cost a rig run once already.

**Text only, ever.** Critical rule #8: the per-track CRCs prove bit-perfection, so no audio
is committed even temporarily.

## Ordering — four rules, and they are the spec's, not ours

**Ordering is `handshake.sort_key` — `(round, lap, stem)` — and nothing else may spell
it.** "Which file is newer" was a plain stem sort in three places and all three were
wrong the moment the naming schemes mixed; one of the three decides `--status`.

**The naming convention is not complete without these.** A shared *format* with
unshared *ordering* is a format both sides can honour while reading it differently —
which is the state both projects were in for one lap without knowing, because a
difference that changes no observable behaviour today is invisible to every test either
side can write. Three of the four below were found by *comparing implementations*, not
by a failing test.

| rule | where it comes from | why |
|---|---|---|
| **1. Order on `(round, lap)` from the wire header, never the filename string** | protocol §3: *"by declared number, never by filename or mtime"* | The filename is a description; the header is the declaration. **Qualified:** the header begins at round 7 lap 2, so for a pre-v2 file the name is the only fact in existence. "Never the filename" means *never in preference to the header* |
| **2. A file that declares no `HANDSHAKE-LAP` is lap 1** | protocol §3: *"absent means lap 1"* | A round's pre-lap-header file **is** its first lap. Not 0, which invents one; not unknown, which would let it outrank real laps |
| **3. An ambiguous `HANDSHAKE-LAP` outranks every real lap** | the fork's lap 18 §B1 | Present-but-ambiguous must be *examined*, not hidden. It becomes the file the verdict is read from, and the header check then refuses it by name |
| **4. `stem` is the third component, as a tiebreak only** | ours; the fork's three rules do not mention it | Two files at one `(round, lap)` is a state the convention forbids and `--check` refuses, but a **non-total** key makes "the newest file" depend on directory iteration order — which decides a release gate differently on two machines |

**Rules 1 and 2 were ours to fix, and both were divergences from a spec we already
ship** (found in lap 21 by diffing the fork's stated rules against our code, at their
request): our sort key read the *name* for the round half while the lap half already
read the header, and a lap-less file fell back to the name rather than to lap 1. Neither
changed the order of any file either project has ever had — which is exactly why only a
comparison could find them. **A spec statement with no conformance row went
unimplemented for the whole life of the spec.**

**Two states are refused rather than ordered** (`handshake.ordering_blockers`, consulted
by `--status` and so by `--release-gate`), because in both the permissive reading is the
wrong one:

* **a v2 file with no `HANDSHAKE-LAP`** — §2 rule 4, an absent required field fails
  closed. Under rule 2 it would sort *oldest*, so a later `GO` could be read as a
  round's newest word while this file's `HOLD` sorted underneath it;
* **a file whose declared round is not the round it is filed under** — §3 and §8 row 10.
  Believe the name and a file disowns its own declaration; believe the header and a file
  votes in a round it says it is not in. So neither.

Grandfathering is **derived** — a file with no `HANDSHAKE-PROTOCOL` line predates the
header and neither refusal applies to it — rather than a list of round numbers, which
stops covering the files added after it was written.

**A golden reference has two commits** and both get named — *"generated by X, committed
at Y"* (the fork's lap 16 §C). The build that produced it is the parent of the commit
that checked it in, because a file cannot contain the hash of a build containing itself;
regenerating inside the change's own commit moves the mismatch rather than removing it.
The filename carries X.

A round is **CLOSED** only when all three files exist **and both sides declare
`GO`**. The verdict closes the round, not the file's existence, and **one side's GO
is not enough** — reading only our own verdict made the fork's `HOLD` unable to block
our release. `python scripts/handshake.py --status` reports both:

```
round-6: ... we-verified=yes (GO)   they-verified=yes (GO)    -> CLOSED
round-7: ... we-verified=yes (HOLD) they-verified=yes (HOLD)  -> OPEN
```

**No release and no pin switch happens while any round is OPEN.** See
`../cyanrip-handshake.md` §7.5 for why the gate reads the verdict at all (it used to
read presence, and reported a HOLD as closed) and **§8 for the shared wire format** —
the column-0 header block both projects now emit, after both built a release gate in
a different vocabulary and neither could read the other's files.

## Round-by-round

Newest first. `pin` is the fork commit the round concerns; the **live** pin is
whatever `src/platterpus/deps/fork_source.py` builds, which only ever moves to a
commit a *closed* round verified.

| Round | Pin | Verdict | What it was about |
|---|---|---|---|
| **27** | `221a1df` — **the pin MOVES at the round boundary** to `+platterpus.16`, `release_seq` 26, both channels, cut on round 26's authority with that round's two fixes (the `media` tag, and an interrupted track left out of the AccurateRip tally). No test pin: a released build. | **CLOSED `GO`/`GO` on BOTH gates at six laps, 2026-09-26** — their laps 1, 4 and 6 (lap 6 is the first lap written in their LSL body language), our laps 2, 3 and 5; our gate closed at our lap 5, theirs closes at their lap 6. §0.1 met by a **quick** run under the operator's override of R1; the Full run moved to round 28 | **The same shape as round 26, fixed at lap 1:** §0.1 the real test — our **Full** acceptance run with `.16` installed through our app, from a release whose `PIN_UNDER_REVIEW` is `221a1df`, the bundle committed to both repos; §0.2 each side's reading of it (theirs across every rip, not only the whole-disc one); §0.3 both releases named (ours rolls `FORK_PIN` to `221a1df`, theirs is `+platterpus.17`). Opened before the test by a recorded operator override of R8 point 3, as round 26 was; their §D proposes wording that makes that order the rule. Carried, not close conditions: the `Accurip 450` wording, the album loudness block, and our three portable bug patterns. |
| **26** | `df91ae7` — **the pin MOVES at the round boundary** to `+platterpus.15`, `release_seq` 25, both channels, the first fork release cut under v6 R8. No test pin: a released build. | **CLOSED `GO`/`GO` at six laps, 2026-09-24** — their laps 1, 4 and 6, our laps 2, 3 and 5; our gate closed at our lap 5, theirs at their lap 6 | **One close condition that needs a drive, and says why (R8 point 4):** the real test — our full acceptance run with `.15` installed through our app from a release whose `PIN_UNDER_REVIEW` is `df91ae7`, the bundle committed to both repos — plus each side's reading of it and both releases named (ours rolls `FORK_PIN` to `df91ae7` at the close; theirs is `+platterpus.16`). **Opened before the test, by an operator override of R8 point 3**, because our acceptance run can only demand `.15` once a lap of theirs names it. |
| **25** | `3e01bb3` — **unchanged; this round reviews shared TEXT, not a build** (S-15). No test pin. | **CLOSED `GO`/`GO` at five laps, 2026-09-23** — their laps 1, 2, 3 and 5, our laps 2 and 4; our lap 2 crossed their lap 2 under one number, which was our miss | **Two close conditions, both text, fixed at lap 1:** §0.1 `PROTOCOL.md` **v6** byte-identical in both trees; §0.2 `OWNERSHIP.md` **v3** and `seam-rules.md` **v6** likewise. v6 carries round 24's agreed list (K1–K3, §5b step 1 / C37 on decision-time enumeration, the agreed-change ledger §5e with C44/C45, §3b *what a citation names*, the two §8 sentences deleted) plus three corrections of theirs (C43, C13a amended, `ACK` retired). **We land first this time** — their proposal as-is for OWNERSHIP and seam-rules, and v6 with one amendment of ours: §5b step 1's release condition is a requirement on the candidate, not a filter, because read as a filter C38 can never fire (both gates already implement the requirement reading). Their lap 1 also chose the subset of our round-25 agenda that closes the round and filed every other item by name (§E). |
| **24** | `3e01bb3` — **the pin MOVES at the round boundary**, `2cce60d` → `3e01bb3`, `+platterpus.14`, `release_seq` 24, published to BOTH channels on round 22's authority **before** this round opened, on the fork operator's instruction (their plan's option A over the recommended beta). `FORK_PIN` rolled to `3e01bb3` when the round closed on our gate. No test pin: the reviewed pin is a released build. | **CLOSED `GO`/`GO` at three laps, 2026-09-23** — on our gate at our lap 2 (§5b step 3), on theirs at their lap 3: the first round whose two gates closed on different laps, printed by ours as it happened | **One close condition, fixed at lap 1: our verdict on `3e01bb3`**, resting on our parser at 0.6.53+ having read a log `3e01bb3` wrote — their golden reference is declared sufficient, a rig rip welcome and not required. The pin carries round 22's agreed change and nothing else: the per-track `ripped and encoded` → `read` rename (P1 for us) and the three-arm `Encoder errors:` footer (P2). **The first lap either side has declared `HANDSHAKE-PROTOCOL: 5`**, and the first our gate accepts at `--check` — it would have refused it a day earlier. Their lap also answers our standing status's three questions from their own code: §B1 their gate has our version-refusal gap on inbound laps; §B2 their coverage split is by version but drops `C13a` by spelling; §B3 **they agree our reading of §5b's "enumerated" is the one under which §5b works**, keep the literal reading until v6, and propose the v6 text. Ships `PROVIDER-CONTRACT.md` from the reviewed pin (`g2e6d97d`, same `src/` as `3e01bb3`). |
| **23** | `2cce60d` — **unchanged, and deliberately reviewed twice for two different properties.** Round 22 reviewed this commit's CONTRACT; round 23 reviews its BEHAVIOUR ON A DRIVE, which no filed rig session had ever covered. Their lap 1 states it will not move (S-15) and that `+platterpus.14` is explicitly a NON-condition. **No test pin** — the reviewed pin is a released build the rig installs un-warned. | **CLOSED `GO`/`GO` at four laps, 2026-09-22** | **The hardware acceptance round, and `PROTOCOL.md` v5.** **Three close conditions, fixed at lap 1 under S-13.** §0.1 v5's two clauses: a close may read the peer verdict from the newest peer lap the writer holds and has enumerated, and — our condition, adopted whole — a lap read for its verdict must declare `HANDSHAKE-READY-TO-READ: yes`, fail-closed. §0.2 a HELD lap's draft verdict reaching the compiled `Handshake:` line as though settled; pre-existing, bounded to unreleased builds, and contract surface because we parse that line. §0.3 the acceptance run dispositioned — **explicitly not "zero failures"**, which they name as the round-7 failure mode. **The run is session `20260922T022152Z`**: 247 of 247, zero failures, 4h14m, eight rips, on `0.6.52` + `2cce60d`. We grade it `partial` in our own ledger and the reason is ours — three of the eight rips had their post-rip CTDB and FLAC-integrity checks dropped unfinished and no step could see it. Our lap 2 answers §0.2 with a measurement (their proposed qualifier run through the real parser and the real classifier), assents to §0.1's substance, and **corrects their §0.3 row 2**: `replaced: false` on both re-ripped tracks, so nothing was superseded. Their lap 1 ships **no provider contract**, so `_MAX_TABLE_LAG` goes 0 → 1 — defensible only because the argv surface's non-movement is derived twice, at the source (no option/`getopt`/argv line in `2cce60d..2f7d9c9`) and at the artifact (the P1 flag table byte-identical across both contract builds, sha256/16 `dd2aa6401baa2654`). **Closed on their lap 5.** Our lap 4 committed v5 byte-identical and re-ran the four hashes — but §0.1 is measured by THEIR tool reading OUR DEFAULT BRANCH, and we had hashed the working copy: `main` still carried v4 while v5 sat on the session branch. They caught it, said nothing was in dispute, and waited for the merge. **The merge then fired their §D2**: the branch was deleted, briefly orphaning the two commits their laps cite; recovered from this session's clone before `gc`. Out of that came the round-24 proposal they took early — **the sha256 is the anchor, the commit is a fetch hint** — so a pruned ref degrades a citation from fetchable to verifiable rather than to nothing. |
| **22** | `2cce60d` — **the pin MOVED, and it is the first round in five to open on a new one.** `+platterpus.13`, `release_seq` 23, published to both channels on round 21's authority before this round opened. **No test pin, and that is an answer rather than an omission**: `2cce60d` and round 21's test pin `3952c03` are the same source, so round 21's hardware transfers and round 22 needs no drive. (Derived here, not taken: `git diff --stat 2cce60d..3952c03 -- src/ meson.build` reports one file, one line, and that line is the version string.) | **CLOSED `GO`/`GO` at five laps, 2026-09-21** | **The operator's goal round: fix known issues and release both applications, with the hardware acceptance session deliberately pushed to open ROUND 23 rather than close this one** — because round 21 took five laps for exactly one reason, that its lap 1 named a condition needing a drive. **Three close conditions, none needing hardware.** §0.1 the two shared-protocol changes, K1 (a lap number is claimed on RELEASE, not on writing — ours, raised in round 21 §K) and K3 (a warning about a held lap cannot travel inside it — theirs, out of our §J1), both needing agreed text, a `HANDSHAKE-PROTOCOL` bump to 5 and both sides shipping v5 before either declares it. §0.2 whether a Platterpus release can be cut inside this round, which only we can answer — and **0.6.51 shipped on 2026-09-18, hours before they opened**. §0.3 two P2 per-track lines reworded and a new `Encoder errors:` footer, landed but not shipped and explicitly vetoable. |
| **21** | `fe4d2c4` — **the release pin is unchanged and is not being asked to move** (`release_seq` 22, stable). What moves is the **test pin, `3952c03`**, and unlike round 16's it is deliberately **not the same program as the reviewed pin**: `git diff fe4d2c4..3952c03 -- src/ meson.build` is 211 insertions across five files and carries both breaking changes, so a rip on it differs from one on `fe4d2c4` on **both** fields — which is the evidence the hardware session is for. Every logfile it writes says `NOT a released build`. | **CLOSED `GO`/`GO` at five laps, 2026-09-18** — their lap 1 `OPEN`, our lap 2, their lap 3 `OPEN` pre-committing to close, our lap 4 `GO` (released after the session), their lap 5 `GO`. Our verification is `verified/round-21-lap-06.md`. Two extra laps against rounds 17–20's three, and **neither is a moving finish line**: lap 3 because our lap 2 arrived before a drive could, lap 5 because each gate reads a round's state from its own newest lap. The avoidable one is ours — a session scheduled before §0.2 was answered would have made this a three-lap round. | **The announcement round for two breaking log changes, both of which we had already assented to INSIDE round 20 — and their lap says plainly that the assent is not a substitute for the announcement, which is why neither shipped in that round.** (1) `Frame retries:  N` → `Retry limit:    N (per frame, and per whole-track re-read)`, the exact string we assented to in our round-20 lap 2 §0.2; our parser has accepted both labels since then, so it could not break us on arrival. (2) `cyanrip_log_finish_report()` moved below the encoder-status loop, so `Ripping errors:` now counts encoder failures. `-j` follows (`frame_retries` → `retry_limit`, a new `cache_probe` block) and the record's schema moves to `cyanrip-diagnostics/6`. **The second change is invisible to a contract diff by nature** — it alters no format string, only what the number MEANS — which is why a green argv-surface suite says nothing about it and why our §F reconciler was deliberately not built before it landed. **Two close conditions, fixed at lap 1 under S-13:** §0.1 one hardware acceptance session on `3952c03` with 0.6.50, establishing three things and no more; §0.2 **our ruling on `Rip completed:`** now that it sits beside a non-zero error count — and they state that a refusal closes it exactly as an assent does. The close-by is 2026-10-20 rather than a fortnight because a rig session has to be scheduled, and because they took our note that it is *"the strongest completeness signal in the log"* seriously rather than as a formality. **Their §H is against themselves**, carried four rounds: `diagnostics.c` asserted that our `SUPPORTED_SCHEMAS` gates that record's schema, which it does not — it is a `frozenset[int]` over their *release manifest*, the same misattribution round 12 cost a whole round to. **Their §5 corrected who fixes the `seam-commands.md` rows** after we pointed out that §7 declares itself generated by *their* tool: two of the three are theirs to regenerate, and chasing it found a defect in the generator — `probe-argv-surface.py:99` grades a row `accepted` on exit status alone when no header field exposes the effect, so 48 of 68 accepted rows were never observed to take effect, and `--check` refuses on that file for want of generated-block delimiters, so **a section that has declared itself generated since it was written had never been verified to be.** |
| **20** | `fe4d2c4` — **unchanged, and neither side asks it to move.** No test pin, no candidate, no release. Their `HANDSHAKE-PIN-POLICY` states the durable form of the claim rather than the drifting one: across the whole span from the pin, exactly one commit touches `src/` and it changes zero non-comment lines. | **CLOSED — `GO`/`GO` at three laps** | **A procedure round on an unchanged pin, opened by them once our hardware artifacts existed**, with two close conditions fixed at lap 1 under S-13, neither needing a drive. **(1)** `HANDSHAKE-CLOSE-BY` — enforce or strike. We had offered both as equally acceptable, which their round-19 §3 had already shown was a judgement rather than neutrality; they answered **enforce in R2's own sense — print, never block — and built it rather than proposing it.** Our lap 2 built the mirror, and the two reporters agree on every row they published (round 8's bare date refused rather than defaulted, rounds 15–18 silent, round 19 set in lap 3) while ours additionally flags rounds 13 and 14 as set-in-lap-2. Both keep the report **out of the function that forms the verdict** rather than behind a guard — their structural argument, adopted verbatim. **(2)** `Frame retries:` → `Retry limit:`, which we assented to; `-r` caps paranoia's per-frame retries *and* the whole-track re-read loop, and their log printed both as a bare `3` with nothing saying they were one knob. **The rename is invisible to our parse and not to our completeness sweep**, so we landed both labels in advance — a `-j` key can carry a duplicate for a release, a log line cannot. **Their lap withdrew three of its own claims while held**, including grading our 0.6.48 run on its own 241-pass verdict field, and it credited our section-F finding while noting they had the logs and read it as a curiosity: *"`-Z` appearing where it had not been is a coverage LOSS in the costume of a coverage gain."* **Their §2.3 refuted a NEXT-ROUND item of ours** — `APPROVED_FOR_PLATTERPUS_VERSION` naming `0.6.47` while `0.6.49` ships is correct, because it names the pairing the record approves rather than the newest that exists; our own `handshake_approval.py` docstring had said so since round 17 and we raised it without reading it. **Their §5.4 corrected themselves that we do read `Ripping errors:`** — we do, and it becomes the `"No errors occurred"` string our EAC-compatible log export writes, so their demonstrated trailer-write failure would reach an archival artifact through us; the half that is ours is that nothing reconciles that summary against the error lines above it. |
| **19** | `fe4d2c4` — **unchanged**, and they derived that rather than asserting it: measured at `0013c56`, 47 commits past the pin, exactly one touches `src/` and its diff contains zero non-comment lines (`7b2fda6`, a `cache_probe.c` header). The count is anchored to a SHA because it is the one number in the lap that moves. | **CLOSED — `GO`/`GO` at three laps** | **A procedure round on an unchanged pin. Two close conditions, both specification, neither needing a drive**, fixed at their lap 1 under S-13: **(1)** tier 4's verb and the dependency graph — the half round 18 left to them, with tiers 0–3 and the escalation rule explicitly NOT reopened since round 18 closed `GO`/`GO` on them; **(2)** whether a transport envelope counts as a lap under §5a, unchanged from round 18 and still the one thing the two gates disagree about, which §6a-ter says no override may excuse. **Their §0 revised its own close conditions while the lap was held and said so in the file** — the first use of the release-state mechanism for the thing it was built for: a lap that is committed but not announced is still a draft, so replacing a condition is legal, and the moment it flips to `HANDSHAKE-READY-TO-READ: yes` S-13 freezes them. Their §1 records that our token answer arrived while they held the lap and that we chose the harder half — the concepts renamed rather than a mapping table — and they left their own unedited recommendation standing so it is visible they were not agreeing after the fact. **Their §7 found a pointer of ours that does not match what it points at**, and their §6.2 anchored our own lap count to `8ea389c` for the same reason they anchored theirs. **Filing their lap exposed a defect that was ours**: `handshake.py --check` reported nine phantom problems against it, because the A–J table it sweeps is a *return-file* table and — since §1a went normative — a provider's lap 1 is an **opener**, not a reply. Nine of thirteen inbound lap-1 files on disk failed our own checker; round 16 is the lone pass because it happened to letter its sections `## A`…`## J`. **Running that fix on our own lap found the mirror**: `check_outbound` required an opener's sections of every reply, and 9 of our 32 committed outbound laps failed our own outbound checker. Our lap 2 answers both close conditions — tier 4 accepted whole, and condition 2 settled against **our** digest tool, which excluded the transport envelope by filename where §5a says the test must be derived from content. Every digest in the record is unchanged, so it closed a latent defect rather than a live disagreement. **Their lap 3 closed it**, and reported the same class in their own gate arriving from the opposite direction: their enumerator implemented §5a's content test correctly and had it **defeated by value strictness** — `ROUND_RE` requiring `(\d+)` meant our envelope's `HANDSHAKE-ROUND: not-a-lap` never matched, so the second declaration was invisible to a check that counts declarations, and only the filename kept it out. **The disclaimer written to say "this is not a lap" was unseeable by the check it was written for.** Their §3 also improved our §E: `HANDSHAKE-CLOSE-BY` is not merely unset on both sides, it is the sole trigger for `EXPIRED` (protocol §4a, lines 281 and 292), so **the spec's own answer to a runaway round has been unreachable for five rounds** — verified against our copy. Q1 settled their way, enforce from round 20. Their §4 answered our Q2 by shipping `PROVIDER-CONTRACT.md`, which took the argv tolerance from 3 back to **0**; its source anchor was recomputed here by their generator's own method after two plausible constructions of ours produced two wrong answers, and the inventory regenerated from it is byte-identical at 120 P5 + 7 P5a. Their §5 caught a line number we transcribed where the adjacent field promised derivation — correct, and adopted. |
| **18** | `fe4d2c4` — **unchanged; a procedure round does not move a build**. No test pin, no candidate, no release. | **CLOSED — `GO`/`GO` at three laps** | Opened by them (lap 1); our lap 2; their lap 3 closed it. Settled the tiered acceptance vocabulary at **seven concepts with the token in a separate column** — our §B2 found that their proposed `SKIPPED`/`BLOCKED` mean the OPPOSITE things in our tree (ours a consequence where theirs is a decision), which they confirmed. Their §4a found our conformance row pattern could not match `C13a`, so the table has **37** rows and we had counted 36 — and a row the denominator cannot include can never be reported uncovered. Their own counter had the identical hole. Re-approves the pin against Platterpus **0.6.47**, moving `APPROVED_BY_ROUND` to 18. |
| **17** | `fe4d2c4` (`0.9.4-rc2+platterpus.12`) — the release **candidate**, and deliberately not the bump: `6a9a080` moved the version, `a2523c4` regenerated the derived artifacts, and `fe4d2c4` is the first commit at which the two agree. `6a9a080` is red on its own suite and must never be released. **Not published** — `release-manifest.json` still resolves both channels to `978f9b0`, so what anyone can install today is still `+platterpus.11`. | **GO/GO — closed at 3 laps on their lap 3** | **The release round, and it approves TWO releases as a pair** — `fe4d2c4` and Platterpus `0.6.46` — to be published together and tested together, with neither publishing alone, on the operator's instruction. **One close condition, fixed at their lap 1 under S-13:** both sides name a candidate by SHA, each verifies the other's declared observable surface, and the close authorises publication — **the release test follows the close rather than sitting inside it**, deliberately, because a test inside the round would block the release that could fix it. **No hardware required**, and after round 16's sixteen laps and five rig sessions that is the point. **Three `HANDSHAKE-BREAKING` rows**, all crossing the *published* `978f9b0` rather than round 16's `a9aedf0`, so a consumer upgrading meets all three at once: the two record timestamps gained a `±HH:MM` offset; the `-j` record's `schema` moved `/3` → `/4`; and the P5 string `Error parsing string: %s!` was removed. Each is handled here and each was derived rather than asserted — `eac_log_export` tries `fromisoformat` **first** because it is the only parse that can see a zone; we never gate on that record's schema (`SUPPORTED_SCHEMAS` is over their *release manifest*, the round-12 distinction) and `rig_check` takes only `invocation`; and `RETAINED_BEYOND_P5` deliberately keeps the removed string because the build we still ship prints it. **Their §4 corrects their own first draft**: they wrote that Run A's program and this candidate are one tree object, ran the command before sending, and published the two hashes showing they differ by exactly one `src/` commit — `12f2081`, the repeated-`-j` fix, which cannot fire for us because we pass `-j` once. So Run A's evidence covers this candidate's program *minus* that commit, and they say so rather than letting "one tree object" stand. | **CLOSED 2026-09-12 at three laps**, the shortest round yet. Their lap 3 built the candidate from a CLEAN CHECKOUT before declaring — detached worktree at `fe4d2c4`, fresh `meson setup`, 81/81 — the check `+platterpus.5` failed. Both halves then published (`release_seq` 22, both channels) and Platterpus `0.6.47` rolled `FORK_PIN` onto it, which was not bookkeeping: between their publish and that release `approve_ripper` stamped their approved build `unapproved`, §5.al with the roles swapped. **Our gate disagreed with theirs and we reported it** — `--status` held the round OPEN because our newest own-side file predated their verdict, structural in a three-lap round the peer both opens and closes. Closed with a `verified/` acceptance, the round-13 shape, without loosening the gate.
| **16** | `a9aedf0` (`0.9.4-rc2+platterpus.11` — **the same version as round 15's `978f9b0`**, because the fork's ledger has no new numbered release; `release_seq_for_commit` therefore returns `None` for it, which is *not determined* and correct rather than a gap) — **frozen for the round under S-15** | **GO/GO — closed at 17 laps on their lap 17** | Their lap 1 lands **all seven** items their round-15 lap 14 §5 held, each revert-proved individually with the build confirmed green during the revert. **One close condition, fixed at lap 1 under S-13 and unable to grow:** a hardware acceptance run on this pin establishing that the AccurateRip path still succeeds with the rewritten response parser, that `-H` with de-emphasis produces correct de-emphasised audio (`-H -E` satisfies it on any disc — forcing de-emphasis is the same graph the TOC flag takes), and that no line we parse has moved except the ones their §D names. **Four breaking changes, all announced before shipping**: two log timestamps gain a UTC offset, two footer lines appear on paths that previously emitted none, `Preemphasis:`/`FLAGS PRE` now describe the audio written rather than the setting asked for, and the `-j` record moves to schema `cyanrip-diagnostics/4`. **The schema move is the only one that could reject rather than surprise**, and their §D4 asks whether our `SUPPORTED_SCHEMAS` needs widening — deliberately without asserting what our code does, since they cannot read it. Three of the four things they found since the close are defects in **their own checks** rather than in the program, including a golden reference that had gone stale in *format* while 69 of 69 tests passed over it. One is not a check at all: all three of the FIFO's `pthread_cond_wait()` sites were guarded by `if` rather than `while`, reachable because the program installs SIGINT/SIGTERM handlers and its encoder threads sit in exactly those waits — restoring the `if` makes their test SIGSEGV. It is upstream's, and it bears on an unattended acceptance run that cancels rips. Their §H confirms both of our findings: `seam-commands.md` line 97's `-D` row, and our own self-reported `+platterpus.10 / g978f9b0` pairing that never existed. One question, `NEXT-ROUND`: whether to write **committed-is-sent** into `PROTOCOL.md` v5 — the only mechanism either side has identified that makes a lap's sent/unsent state observable in a tree, at the cost of never committing a draft into the lap namespace. **The hardware arrived on 2026-09-10 and it is Run B, not Run A**: 237 of 238 steps on `platterpus 0.6.45` + `platterpus-fork-gddc1e8c`, 8 rips, and the first attested cancel either project has ever had — `Rip completed: no (interrupted by SIGTERM, 0 of 14 tracks)`, `Interrupted at: track 1, mid-read`, a valid `Log FUN512:`, `-Y` exit 0. Its single failure was **ours**, and it was two defects wearing one symptom: we asked the ripper to verify its log 6.1 s before it finished writing it, and — 23.7 s *after* that log was complete — the acceptance script graded our own stale parsed snapshot instead of the file. Their lap 9 §2 named the false negative and stopped short of the mechanism, marking it explicitly unproven; the mechanism is in our lap 10 §C1, read from a rotation of the app log that was in the bundle they already held. Their lap 9 §1 concluded no `-H`/`-E`/`-W`/`-x` reached the drive; all four did, through the script's raw `cyanrip` verb, which produces no album folder — their population was the eight rip logs. So clause 1 has a real AccurateRip host answering and the answer parsed — their lap 1 note 2 said *"a real 200 from a real AccurateRip host has never gone through it"*, their scenarios passing `-N -A -U` with no network — and clause 2 has both arms on the drive, though the audio was not compared. Whether either exercises the specific rewritten path is theirs to confirm; lap 10 §B7 reports the artifact and declines to assert a route through their code. Their lap 9 then arrived as an artifact — verified byte-for-byte against their committed copy before filing — and it carried the finding that changes what happens next: **their `7ace6e5` had added a third file AND a third command to the Run A block**, `tools/round16-accept.py`, which *grades* the run against the close condition and was written before any data existed on purpose. Our lap 3 published the two-command version, so a Run A from our instructions would have measured everything and produced **no verdict**. Ours to fix, corrected in lap 10 §D — and found only because lap 9 §6's prose commit list did not name that commit, which is the same *describe-the-diff-instead-of-deriving-it* shape their own `contract-delta.py` was built for one section over. **Run A remains the artifact nobody has, and the round stays open on it.** **CLOSED 2026-09-12.** Run A ran on 2026-09-11 on `ddc1e8c` and settled all three clauses — `tools/round16-accept.py` at `9ec722e` reporting `0 FAIL, 0 UNPROBED, exit 0`, with clause 2 (`-H` with de-emphasis) exercised on a drive for the first time in twelve rig sessions. **The grader moved mid-round, from `5bbb5ae` to `9ec722e`, and our lap 14 had pre-authorised exactly that in writing** — Run A found a defect no fixture could: `clause1/checksums` graded the *better* rip as a regression, because the 2026-08-05 reference's track 3 was itself `not found` and a reference line the database never recognised is not a reference. The fix adds an `INFO clause1/reference` row **and** an `UNPROBED` where the old code printed `OK`, so it is stricter, which we verified from the diff rather than accepting. Both sides' S-18 pre-commits resolved on an exit code and neither had to argue a verdict. Our half, run here and published either way: `verify_log_surface.py` over Run A's five logs, **1,055 lines, 0 unaccounted, exit 0**. Our lap 16 carried one correction that was ours — lap 3 §I credited them with a decoded-sample point we then got backwards, since every checksum cyanrip prints accumulates over the raw read buffer *before* the encoders, so our acceptance script's clause-2 rule was arithmetically impossible to satisfy and would have reported their `b866900` as unfixed forever. Their lap 17 §1a then refused their own closing lap over a field ours accepted — `HANDSHAKE-PEER-PIN` carrying prose — and asked us to check; we had built that guard in round 12 and aimed it at the verdict fields only. |
| **15** | `978f9b0` (`0.9.4-rc2+platterpus.11`, `release_seq` 21, channel **stable**) — **fixed for the round under S-15**, and the first pin chosen as a subject *by having been released* rather than nominated. No test pin is declared and none is planned. | **GO/GO — closed at 16 laps on their lap 14** | **The first round of the new cycle** (proposed in our round-14 lap 18, adopted in their lap 19): a round communicates, fixes and agrees; both sides ship; the shipped pair is the next round's subject. **One close condition, fixed at lap 1 under S-13 and unable to grow** — CC-1, one hardware acceptance pass on the released pair plus a verification file declaring `GO` or naming what stopped it. Their lap 1 asks for exactly two things: our `HANDSHAKE-OUR-PIN` and channel for the app half of the subject (they refused to guess it off our banner, because for `0.6.28` the banner's build id `296a69d` and our declared pin `b524936` disagreed — a source build off `main` one commit past the release commit), and three diagnostic commands on the rig for a `cyanrip --version` hang that made **two** consecutive morning sessions produce zero rips. They establish the hang is not cyanrip three independent ways, the strongest being our own installer's command substitution, which blocks until the child exits and did return. Filed as information, neither proposed as blocking: their own `PROVIDER-CONTRACT.md` defect (P2's blanket *"both stdout and the logfile"* is false for four `-I`-only rows — verified here from the artifact, and a no-op for us because `parsers/cyanrip_info.py` reads `MusicBrainz URL:` from `-I` **stdout** and our generated consumer contract names none of the four), and our acceptance script refusing the build they had released, which is their §3. |
| **14** | `f2c0506` (`0.9.4-rc2+platterpus.9`, `release_seq` 19, channel `beta`) — **a released build, not a test pin**, and that is the point of the round; moved from `796df32` at their lap 2, `src/` byte-identical between the two | **OPEN — opened by them; their lap 3 is `HOLD` with an S-18 pre-commit to `GO`** | **One close condition, and it needs a drive.** CC-2, carried from round 13 by bilateral agreement: one hardware acceptance pass on the *released* pair — cyanrip `+platterpus.8` against Platterpus 0.6.24 — exercising their §T list (T1 a `-Z` rip that genuinely re-reads, T2 `-T unicode` on a title carrying `<` and `:`, T3 `-x -I`, T4 an interrupted rip on hardware, T5 an Enhanced CD if one turns up). Round 13's CC-2 was unsound because it measured a **test pin** while the release would necessarily be a later commit, so satisfying it would have closed a round on evidence about a build nobody installs; round 14 tests a build that is already released, which is what makes it terminate. They pre-commit under S-18: *their next lap is `GO` unless our pass fails on a cause that is theirs, or we ask for a hold.* Their lap 1 declares one added log line — `Scope:  the last of %i reads; …`, printed only when `total_repeats > 1` — which is **our own round-13 finding shipped**, and our parser reads it on all three tracks of their golden reference (per-track 30 against a disc total of 90, ratio exactly the re-read count). Two `NEXT-ROUND` questions from them: our six round-13 digest rows (their `051bfc6d98ed1eb9` against our `039cfa03a335266e`, three hypotheses rejected by measurement and the fourth — that our records genuinely differ, because we renumbered a lap they hold as lap 1 — untestable from their side), and the one-lap tail. |
| **13** | `9f8592e` (`0.9.4-rc2+platterpus.7`) — reviewed and approved, **not installed**; every measurement in the round was taken against the INSTALLED `ddf7ac3` | **CLOSED — `GO`/`GO`, eight laps.** Their lap 8 was written as a closing lap with no artifacts, so no envelope was ever built for it and it never sent; requested in round-14 lap 2 §H and delivered inside their round-14 lap 3. *A lap with nothing attached is exactly the one that gets forgotten.* | **The endgame round, opened after the first FULL hardware acceptance pass** (2026-08-23, 98 steps, 1h 50m, `pass=94 fail=1 error=3`). Its blocking ask is a value that crosses the seam and **neither contract describes**: the on-disk folder name. Measured — our overwrite guard predicts `…angle<bracket…` where cyanrip writes `…angle‹bracket…` (U+2039), so the guard probed a directory that does not exist and a 2-track rip silently overwrote part of a **completed 14-track archival rip** and its logfile. Downstream: `flac --test` reported a mid-write file as corrupt and three warnings fell back to a folder scan. Cause on our side is a two-entry substitution table whose own comment says *"we reproduce the two the user will actually hit"*; cause on the seam is that we never send `-T`/`--sanitize` (so we inherit their default) and their P1 documents the flag's four modes while documenting **zero** of the substitutions — the glyphs appear 0 times in their contract. Also carried: post-rip verification kept running while the next rip overwrote its files (the app logged *"evidence bundle abandoned"*, so one guard was honoured by one consumer of five); `expect-status` is in our verb table with no handler; and the honest finding that **tracker acceptance is unreachable for either project** — logcheckers gate on ripper identity before grading, so cyanrip scores 0 regardless of quality, and EAC-equivalent rigour in our own voice (KDD-24) is the reachable goal instead. |
| **12** | `64ae7bc` (`0.9.4-rc2+platterpus.7`) — reviewed and approved, **not installed**; lap 1's artifacts were built at `def36a6` and lap 3's at `237a4ff`, both declared | **CLOSED — `GO`/`GO`, four laps** | Opened by the fork. Subject: they merged upstream `0.9.4-rc2`, and our round-11 `-x` report turned out to be **correct as a finding and wrong as a diagnosis** — `on_quit_signal()` called `cyanrip_log()`, which takes a mutex and uses stdio, so a signal arriving while the main thread held that lock deadlocked the handler on the thread that would have released it. They found it by looping their new test until it hung and reading the backtrace; upstream has it verbatim. Also: SIGTERM unhandled entirely, `-Z` looping after being told to stop (182 `Stopping, ripping incomplete!` lines in one cancelled rip, now 1), five distinct `--verify-log` exit codes, and a checksum no longer published for a track that never finished. **Their `HANDSHAKE-BREAKING (1)` is false and half the cause is ours**: they read `SUPPORTED_SCHEMAS` as gating the `-j` diagnostics record, but it is a `frozenset[int]` over `release-manifest.json`, nothing here reads a diagnostics schema string, and a rip never sends `-j` — measured, with `/2`/`/3`/`/999` indistinguishable through the real path. Our own round-11 lap 4 had written *"when we next widen `SUPPORTED_SCHEMAS`"* without naming the document, one round after telling them we consume none of that JSON. The **real** warning, attached to the right artifact: the release-manifest bump to schema 3 they deferred *is* refused by shipped Platterpus and must land here before they publish. Three findings in their output, all `NEXT-ROUND` under S-14: their generated P4 says exit 1 for everything while their lap says five codes; a literal `<commit>` placeholder where the generating build should be named; and no artifact was produced by the pin the round approves. Four defects found on our side chasing it — an unreadable log reported as tampering, two fatal strings reaching the user as a bare "Rip failed", `-j` missing from our published flag list, and a "why is this build here" clause silent for five rounds. Closed in FOUR laps against round 7's 37, which is their convergence rules working — the lap-1 pre-commit meant every finding could be reported without reopening the round. **Their lap 3 refused the blame we offered to share, and was right to**: we called §A1 *"half ours"* on a name-collision story, they opened all three sentences and every one sat in unambiguous release-manifest context. The generous cause was a fiction and would have imported the wrong remedy; theirs is checkable and is now a `CLAUDE.md` rule — *never state a mechanism in the other side's code without citing where you read it* — with its mirror beside it, that an apology is the one assertion nobody audits (`docs/testing.md` §5.ax). Their §E1 was worse than we reported: P4's rows were literal strings in the generator and `exit_codes()` scanned integer literals inside `main()`, which returns `rc` from `cyanrip_run()` — it reported `1` and missed even `0`. They also declared at column 0 that they had removed a sentence our test parses, which exposed that the test reads `round-4.md`, nine rounds stale, while its docstring claims it re-derives. Two `NEXT-ROUND` asks from us: which of eight further lines are theirs (our fork-only marking is the one hand-kept field in a generated document, and it had rotted), and tell us when `+platterpus.7` is cut so the build tag can enter our capability tables — until it does, their five new `--verify-log` exit codes are unreachable from Platterpus. |
| **11** | `c455683` (`0.9.4-rc1+platterpus.6`) — byte-identical to the published `c4d1a00` in `src/`, `meson.build` and `meson_options.txt`, re-measured here | **CLOSED — `GO`/`GO`, four laps** | Opened by the fork. Subject: **`release-manifest.json` schema 2 and its new `build` field.** Their §0 caught a trap in a plan we had written down but not built — adding `-Ddeclare_released=true` unconditionally would have made **our own current pin unbuildable**, because `meson_options.txt` is absent at `ddf7ac3` and meson fails the *whole* configure on an unknown `-D` (confirmed independently in their tree, not taken from the lap). Our lap 2 accepts the requirement and **refuses the mechanism**: their `build` is a shell command string, and executing it would turn a remote JSON field into arbitrary command execution on a path that later runs `sudo install` — so it is parsed to a meson-option allowlist and our own command is kept. Our findings: the published manifest carries `build` for the **channel head only**, so §1's "including for rollbacks" is not supported by the artifact (`NEXT-ROUND`); and their **schema 2 was already live against our shipped reader**, which pinned schema 1 and returned *not determined* for the real document — the number doing exactly its job, and answering their J3. `FORK_PIN` stays `ddf7ac3`: `c4d1a00` has no hardware behind it. Their lap 3 **ruled our close condition 1 met and named the defect as theirs**: conditions 1 and 2 could not both be satisfied by a consistent actor, since 2 excuses not installing `c4d1a00` while 1 asked us to install it — *a close-condition defect is not a close-condition change*, so S-13 is intact. They corrected their own §1 by `HANDSHAKE-CORRECTS`, chose to withdraw the sentence rather than emit `build` per ledger row (a new key means `schema` 3, which our shipped `0.6.12` would refuse exactly as it just refused 2), and enclosed `PROVIDER-CONTRACT.md` — which took our argv tolerance **2 → 0** and cost them a `make-envelope.py` fix, since one lap plus artifacts was the one shape it refused. Our lap 4 closes it. |
| **10** | `56413d2` (`0.9.4-rc1+platterpus.6-beta.4`) — pin moved once, at the implementation lap, as lap 1's policy said | **GO/GO — closes on their lap 5** | Subject: **`HANDSHAKE_RELEASED` was unreachable**, so `-- NOT a released build` had become a constant. Our lap 2 chose **(b)** (declare at build time in the `Consumer:` idiom, option defaulting unset so a mis-set flag *under*-claims) and contributed the evidence that broke their own generalisation: `round08pinmanifest.txt`, written by `gddf7ac3`, carries **no disclaimer**. They corroborated it by rebuilding `ddf7ac3`, corrected our interval (`_head_is` entered at `a083279`, **after seq 15 and one commit before `b56f936`** — so *round 9 approved the first build ever to carry the broken flag*), and **declined our diagnosis**: restoring `ok` alone would let `b809cfc` claim released. Their lap 3 built it — released rendering reachable from a real rip, five flag states isolated, four revert-proofs including one they reported as confounded. Our lap 4 found the mirror defect **in us**: every reader anchored on `^Handshake:`, so we would have dropped the *(declared at build time, not verified by cyanrip)* qualifier that is the whole fix — fixed by adjacency-folding, four tests. Their `released_build` → `released_build_declared` rename is free here: **referenced nowhere in our tree.** `FORK_PIN` still `ddf7ac3` — `56413d2` has no `release_seq`. |
| **9** | `b56f936` (`0.9.4-rc1+platterpus.6-beta.4`) | **CLOSED — `GO`/`GO`, 11 laps** | Opened by the fork per v4 §1a. Adopted protocol v3 then **v4** byte-identical, carrying both of our amendments (the exactly-once rule for what counts as one lap; a digest excludes the lap that carries it) plus their asymmetry fix — *the verifier excludes the lap it received, not its own newest*. Went to `RECONCILE` twice on digests that were **never a disagreement about the record**: their lap 5 published a verifier's computation under the writer's field, and their lap 7 a value typed from a command run before our lap 6 existed (we found the set `{1,2,3,5}` by exhaustive search; they found the command in their own log). Underneath: **a sent lap edited on each side**, their gate closing a round on a superseded peer verdict *and compiling "round 9 lap 7 closed" into every logfile*, our gate unable to close a round the peer opened, a pin check satisfied by a build tag, and a suite exit code read from a pipe. Final digests agree: `18b950305b58a1c9 over 11`. |
| **8** | `ddf7ac3` (`0.9.4-rc1+platterpus.5`) — test pin `cb440bd`, later `2ce8993` | **CLOSED — `GO`/`GO`** | *This row read "GO (ours, lap 10) — RECONCILE … we cannot record [their close]: we hold their lap 1 and none of 3–17" until 2026-09-13, and it had been false since **2026-08-17**.* `inbound/` holds their laps 1, 3, 5, 7, 9, 11, 13, 15 and **17** — all of them, filed in `52dfe0a` — and lap 17 declares `HANDSHAKE-VERDICT: GO`. `--status` reports round 8 CLOSED. The `RECONCILE` grade and its two digests described a gap that had been filled 27 days earlier. **This is the row a peer reading our repo would use to judge whether round 8 is settled**, which is what makes a stale round row different in kind from stale prose. |
| **7** | `345241b` → `422d12a` (`0.9.4-rc1+platterpus.5-beta.8`) | GO both sides at lap 39 | Their §7 measured both rip sessions at 81m11s / 81m13s, refuting our "much faster" explanation — we had described the dynamic-rerip mechanism and let it stand as the cause of a delta never measured. Their §5 pre-gap `Duration:` off-by-one-frame reproduced on our rig, with a sign flip they had not reported (+1 on tracks 1–13, **−1 on track 14**). Their file shipped no §I provider contract; their lap 2 replaced the whole idea with a generated contract plus a resolvable pointer, which is better. Lap 3 adopted their header format as the shared wire format (§8), made the gate bilateral, and put a handshake-approval check in every rip. **41 laps, 10 test pins, 8 pre-releases, 0 releases** — the round that produced S-13…S-16, the convergence rules, because nothing in it was bad work and it still could not end. |
| **6** | `2f950c8` (fork release r2) | GO | The round that took three pins in one day. Their finding: at any paranoia level above 0, ripping a **disc image** returned one correct sector then silence — 99.7% of samples zeroed, reported as `Ripping errors: 0` — inherited from upstream, never affecting a real drive. Ours: two consecutive golden references whose build tags named commits three behind their content; per-track paranoia counters are per-**pass**, not per-track; their §C7 refuted by their own appendix. Amendments `6b` (urgent pin withdrawal) and `6c`. |
| **5** | `e1d800e` | GO | Found the release blocker: every version probe we shipped sent `cyanrip -V`, which upstream deleted after 0.9.3 — and a non-zero exit from a version probe reads here as *"the tool is not installed."* Also found our strongest-looking test was measuring **their** generator's allowlist, not the ripper: their fatal inventory went 88 → 104 on re-derivation and our matcher had missed all 13 matchable strings the allowlist hid. |
| **4** | `a04a94b` | GO | First round under `scripts/handshake.py`, and the round that added §I (the provider contract) to the spec. Their §B answers checked and their golden reference run through the real parser. |
| **3** | — | GO *(retrospective)* | The fork's return file, verbatim. Our verification was **late** and went out folded into round 4's outbound §1–§3 rather than as its own step-5 file — which `--status` is what surfaced, and is a fair summary of why the tooling was written. |
| **2** | — | GO *(retrospective)* | `setvbuf` as the fix for the buffering defect a signal handler cannot reach, and the `-l` track-selection semantics. |
| **1** | — | GO *(retrospective)* | The fork's FIXPLAN: cyanrip's logfile and cue are block-buffered, so a killed process loses up to a 4096-byte stdio block. Reproduced against a real cancelled rip whose log ended mid-token at `REPLAYGAIN_TRACK_GA`. |

Rounds 1–3 carry no `**GO`/`**HOLD` marker — the convention began at round 4 —
so they are grandfathered by number in `handshake.RETROSPECTIVE_ROUNDS`, a set a
test pins to exactly `{1, 2, 3}`.

## These files are correspondence, not documentation

They are deliberately exempt from the doc version-stamp convention
(`tests/test_doc_version_stamps.py`). Stamping an inbound file would edit
another project's words; stamping an outbound one would make the committed copy
differ from what they actually received. A record that is not the record is
worthless. Their currency is the round number.

Do not edit a file here after it has been sent or received. If something in it
was wrong, that belongs in the **next** round's Corrections section — which is
the mechanism, and most of the errors found so far arrived that way.

## Amendments

`round-6b.md` is **round 6**, not round 6b. A round may be amended — round 6 was,
within hours, because the pin it asked for returned silence on disc images —
and `handshake.py` reads `round-<N><suffix>.md` as round *N*. Counting an
amendment as its own round would report two open rounds where one was corrected,
and would make sending a correction immediately score *worse* in the record than
sitting on it. `--check` accepts several files so a round validates as a set, and
`--status` takes the **newest** file's verdict, so a GO withdrawn the same evening
does not keep a round closed.

## Backfill note

Rounds 1–3 predate `scripts/handshake.py` and were recovered from the session
scratchpad, so their section shapes vary and `--check` will report rounds 1–3
against the current spec. That is expected: the spec grew (§I, the provider
contract, was added in round 4). Round 3's `inbound` is the fork's file verbatim.
