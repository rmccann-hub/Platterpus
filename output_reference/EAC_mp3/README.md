# EAC MP3 — baseline (pending)

Empty for now. MP3 output is P1 (v1 is FLAC-only, CLAUDE.md Critical Rule #4).
When we baseline EAC's MP3 reference, add its `.log`/`.cue` here. Note: the MP3
*encode* is lossy and therefore not bit-comparable; only the shared **extraction
CRCs** (identical to `../EAC_flac/`) and tag/structure are comparable. No audio
is committed — see [`../README.md`](../README.md).
